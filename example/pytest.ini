[pytest]
DJANGO_SETTINGS_MODULE = demo.settings
python_files = test_*.py
testpaths = catalog
# The repository root first, so a clone tests the plinta beside it rather than
# one installed elsewhere. A real consumer needs neither line.
pythonpath = .. .
