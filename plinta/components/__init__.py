"""Components: config in, HTML out.

A component is handed a resolved configuration and draws it. It does not know
that saved views, blocks or pages exist, and does not know which other
components are installed.
"""
