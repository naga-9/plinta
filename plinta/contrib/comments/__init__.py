"""Discussion attached to any record.

This app decides who was mentioned; it does not decide what happens next. It
emits `comment_posted` and nothing else — whether that becomes a notification,
an audit row or nothing at all is somebody else's question.
"""
